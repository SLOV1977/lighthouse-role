server {
    listen {{ lighthouse_listen_port }} default_server;
    server_name {{ lighthouse_server_name }};

    root {{ lighthouse_install_dir }}/{{ lighthouse_archive_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}